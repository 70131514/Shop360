// Placeholder firebase config - replace with real config
import {initializeApp} from 'firebase/app';

const firebaseConfig = {
  // apiKey: '',
};

export const firebaseApp = initializeApp(firebaseConfig as any);
